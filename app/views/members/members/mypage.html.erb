<div class="stage-right kamaishi  photo">
  <div class="container">
    <div class="row">
      <div class="mx-auto m-5 col-5 p-0 card">
        <h2 class="text-center m-4">マイページ</h2>
        <div class="text-center m-3">
          直近の練習予定は<%= link_to "#{@most_recent_date} #{@most_recent_practice.orchestra.name} ", members_practice_path(@most_recent_practice) %>です。
        </div>
        <div class="d-flex justify-content-center m-3">
          <%= link_to "出欠確認", members_attendances_path, class:"btn btn-success mr-4" %>
          <%= link_to "プロフィール編集", edit_members_member_path(@member), class:"btn btn-success mr-4" %>
          <%= link_to "入団申請", members_orchestras_path, class:"btn btn-success" %>
        </div>
        <% @member.orchestras.each do |orchestra| %>
          <div class="text-center delayScroll wrapper mb-2">
            <%= link_to orchestra.name, members_attendance_path(orchestra), class:"btn box hover05 m-3" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>